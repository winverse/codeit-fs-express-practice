# Express 서버 시작하기

`src/app.js`의 Express 앱을 완성하세요. `GET /`은 상태 200과 JSON `{ "message": "Hello Express!", "timestamp": "<ISO 날짜>" }`를 응답해야 합니다. 알 수 없는 경로는 404 JSON `{ "message": "Route not found" }`를 응답해야 합니다.

`npm run check:02`가 실제 임시 포트에서 요청을 보내고 서버를 닫은 뒤 종료 코드 0으로 끝나면 성공입니다.
